package com.lifekeeper.app.ui.theme

import androidx.compose.material3.Typography

// All 15 M3 type-scale slots use their spec defaults (Roboto, standard sizes).
// Assign a custom Typography instance here if a brand font or size overrides
// are introduced in the future.
val AppTypography = Typography()
